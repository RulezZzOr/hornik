#!/bin/sh
set -eu

echo "openmineros: NAND writes are disabled in this build." >&2
echo "openmineros: this command is a guard stub, not a NAND writer." >&2
echo "openmineros: verify partition map, bootloader handoff, signed manifest, and rollback before enabling any NAND path." >&2
exit 78
